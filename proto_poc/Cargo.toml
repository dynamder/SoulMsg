[package]
name = "proto_poc"
version = "0.1.0"
edition = "2024"
description = "Cross-language verification for SoulMsg's protobuf payload and hash envelope."
publish = false

[dependencies]
soul_msg = { path = ".." }
smsg_macro = { path = "../smsg_macro" }
prost = "0.14"

[build-dependencies]
prost-build = "0.14"
protoc-bin-vendored = "3"

[dev-dependencies]
serde_json = "1"
