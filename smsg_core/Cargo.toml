[package]
name = "smsg_core"
version = "0.1.0"
edition = "2024"
description = "Shared core for SoulMsg: protobuf-descriptor hashing and envelope framing (language-neutral)."

[dependencies]
blake3 = "1.5"
prost-types = "0.14"

[dev-dependencies]
proptest = "1"
